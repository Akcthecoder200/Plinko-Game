# Environment Variables for Vercel Production
# Copy these to your Vercel project settings

# Database URL - For Vercel, use a production database like PostgreSQL or Turso
# SQLite won't work in serverless environment, you need a hosted database
DATABASE_URL="postgresql://user:password@host:5432/plinko_lab"

# Or use Turso (SQLite in the cloud):
# DATABASE_URL="libsql://your-database.turso.io"

# Server Seed Salt - MUST be a cryptographically random string
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SERVER_SEED_SALT="CHANGE_THIS_TO_RANDOM_64_CHARACTER_HEX_STRING"

# Node Environment
NODE_ENV="production"
